// import * as Actions from './actions'
//
// const setCityName = ( cityName ) => {
//     return { type: [Actions.SET_CITY_NAME], Payload: cityName}
// }
//
// export default setCityName

// event => call handler =>
// handler dispatch an action to redux via {action creator} =>
// call reducer => modify state => notify react =>
// re-render


// 1st thing redux calls rootreducer without state
